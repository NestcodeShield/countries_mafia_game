PORT=3001

# Локальный MongoDB (если Mongo стоит на машине как сервис) — база создастся сама при первом обращении
MONGO_URI=mongodb://127.0.0.1:27017/nuclear_mafia

# Разрешённый источник для CORS (адрес dev-сервера клиента, Vite по умолчанию отдаёт на 5173)
CLIENT_ORIGIN=http://localhost:5173
